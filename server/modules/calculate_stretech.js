/**
 *  Accepts a body like:
 * {
 *  "formula": {
 *    "equation": "['171', '+' '77']"
 *  }
 * }
 *
 * @param {*} formula
 */

function calculateStretch(formula) {}

module.exports = calculateStretch;
